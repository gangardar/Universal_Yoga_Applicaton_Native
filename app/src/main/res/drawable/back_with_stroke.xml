<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/course_background"/> <!-- Background color -->
    <corners android:radius="16dp"/> <!-- Corner radius -->
    <stroke
        android:color="@color/black"
        android:width="1dp"/> <!-- Stroke color and width -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp"/>
</shape>


